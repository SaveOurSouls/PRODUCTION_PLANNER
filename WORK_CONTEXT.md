# Рабочий контекст — Поток / Production Planner

Обновлено: 23.09.2026. Репозиторий: https://github.com/SaveOurSouls/PRODUCTION_PLANNER

## Цель и текущая точка

Создать рабочее место одного производственного планировщика: маршрут изготовления → нормы → назначения людей/оборудования → факт и брак → движение полуфабрикатов. Пользователь утвердил два результата: самостоятельное HTML-демо и серверную v1 на Next.js, TypeScript, PostgreSQL, Prisma и React Flow.

Код обоих результатов создан. Готовое демо находится в `demo/production-planner.html`. Сервер, отдельный worker и база описаны в `docker-compose.yml`. Внешнее развёртывание ещё не подтверждено. Исходники, демо и проверенный контекст опубликованы в `origin/main`, коммит реализации `20b7a4f`. Сохранена история исходного GitHub-коммита `ce5d245`; локальная ветка `main` отслеживает `origin/main`.

## Реализовано

- Русский интерфейс: проекты, ресурсный календарь, схема, нормативы, план/факт, материалы, сотрудники/компетенции, оборудование, операции, шаблоны, архив/корзина и источники.
- Направленный граф без циклов, один Final, ветвления и сборка по нормам расхода. Последовательные операции блока; отдельная расчётная норма и ручная поправка.
- Погонные, статичные и переменные нормы, машинный предел, коэффициенты, независимые исполнители и обязательная бригада. Подготовка отдельно от передаточных частей.
- Смены, перерывы, допуски, количество машин, занятость других проектов, запас и поставки. Резервирование входов при запуске; выпуск доступен после завершения.
- Два режима: варианты первой отгрузки и самое раннее найденное завершение заказа. Расчёт создаёт отдельный вариант, затем применяется с проверкой revision и снимка данных. Есть расшифровка свободных интервалов.
- Факт с частичным/нулевым выпуском, паузами и браком; дневной журнал движений с внутридневными событиями. Нельзя отгрузить отсутствующий запас.
- PostgreSQL, миграция, версии проектов, аудит, очередь worker; API `/api/v1`, авторизация одного планировщика, проверка Origin, шифрование OAuth-токенов.
- Импорт разрешённых Google-листов с предварительным просмотром и диагностикой, экспорт/импорт JSON schemaVersion 1, скрипты резервного копирования и восстановления.
- Автономное демо с localStorage и расчётом в Web Worker.

## Источники и принятые решения

1. `1DZ_H3vIVseQ3LKC8FgrhfezM-4JHznZUFiKvLWhqap0`: только `ЗАГРУЗ`, `Нормы`, `Лист14`, `БД. СОТ`.
2. `1W3VK9Fw71lYdw1Klcsn_za5-2EhvLoXIAKZVYOCnKcs`: только `БД.ОП`, gid `895010191`.

В ЗАГРУЗ номер проекта — E, комментарий — A, календарь — Q. Начало блока сотрудников настраивается, по умолчанию строка 55. Колонки импортируются по заголовкам. Нормы/Лист14 и календарные ячейки сохраняются как референс, а не автоматически утверждённые задания.

Карта компетенций ведётся в платформе: допуск, звёзды, коэффициент. Значения по умолчанию: Пн–Пт, 6,5 эффективного часа, коэффициент 1, настраиваемый часовой пояс, целевой буфер на один эффективный час следующего этапа.

Демо: разделение плат → пайка → отмывка/ОТК → упаковка. Референсные скорости 125/50/545 шт./ч; упаковка, 7000 шт., обезличенные операторы и оборудование — демонстрационные допущения. Не считать демо утверждённым производственным планом.

## Карта исходников

| Часть | Файлы |
|---|---|
| Сущности/валидация | `src/domain/types.ts`, `schema.ts`, `graph.ts` |
| Нормы/расписание | `src/domain/norms.ts`, `calendar.ts`, `scheduler.ts`, `idle.ts` |
| Факт/запасы | `src/domain/actions.ts`, `inventory.ts` |
| Импорт/сравнение снимков | `src/domain/importer.ts`, `serialization.ts` |
| Интерфейс | `src/ui/Planner.tsx`, `RouteEditor.tsx`, `Timeline.tsx`, `Reports.tsx`, `ProjectForms.tsx`, `Catalog.tsx` |
| БД/очередь/Google | `src/server/repository.ts`, `worker.ts`, `google.ts`, `auth.ts` |
| API | `src/app/api/v1/[...path]/route.ts` |
| База | `prisma/schema.prisma`, `prisma/migrations/202609230001_initial` |
| Проверки | `tests/domain.test.ts`, `scripts/integration.mjs`, `scripts/benchmark.ts` |
| Сборка демо | `scripts/build-demo.ts`, `src/ui/demo-entry.tsx`, `src/ui/planner.worker.ts` |

## Подтверждённые проверки

23.09.2026, Windows, Node.js 24.19.0:

- 30 тестов Vitest прошли: формулы, бригада, смены, сборка 2A+B, общие запасы, запрет двойного расхода, пересечения, поставки, два режима, закреплённые/чужие задания, импорт, частичный/нулевой выпуск и брак.
- `tsc --noEmit` и production-сборка Next.js 16.3.6 прошли.
- Prisma Client создан; миграция применена к отдельной локальной PostgreSQL, seed записан/прочитан.
- `scripts/integration.mjs` прошёл: вход, сохранение проекта, очередь, расчёт/применение, отклонение устаревшего варианта, факт, JSON, шаблон, удаление/восстановление и Origin.
- HTML-демо собрано и проверено через localhost во встроенном Chromium: схема, расчёт, причины ожидания, применение варианта.
- Последний benchmark демо 7000 шт. занимал около 12,6 с на этом компьютере, возвращал пять вариантов первой отгрузки. Это измерение окружения, не SLA.
- Перед первым сохранением на GitHub повторно проверены TypeScript и тесты; результаты должны оставаться отделены от непроведённых проверок ниже.

Команды и подготовка изолированной интеграционной базы — в README. `test-results/test-env.json` содержит временные секреты и не входит в Git. Не считать оставшиеся локальные процессы обязательной частью установки.

## Ограничения и ещё не подтверждено

- Docker отсутствовал: сборка образа и перенос Compose на целевом сервере не проверены.
- pg_dump/pg_restore отсутствовали: реальное восстановление резервной копии ещё не проверено.
- macOS/Safari не проверены. file:// заблокирован политикой встроенного браузера; тестировали тот же автономный файл через локальный HTTP preview.
- Живой Google OAuth-импорт требует Google Cloud client и подключения владельцем. Проверки парсера не подтверждают полный обмен с Google.
- Поиск эвристический: несколько размеров партий и два приоритета, без гарантии оптимума. Минимальная перебираемая партия `ceil(объём / 200)`.
- Если любое задание передаточной партии закреплено/начато/имеет факт, сохраняется вся цепочка этой партии. Структурные изменения маршрута с заданиями требуют копии проекта.
- Подготовка начисляется на запуск операции на наборе ресурсов; отдельный интерфейс управления несколькими производственными запусками не выделен.
- Нормы и маршруты хранятся в версионном JSONB проекта; это не отдельные реляционные таблицы каждой операции маршрута.
- Учёт запасов ведётся внутри проекта. В v1 нет глобального перепланирования, параллельных операций внутри блока, автоматической переработки брака и многопользовательских ролей.

## Следующий конкретный шаг

На целевом окружении с Docker выполнить сборку и миграцию, проверить сценарий создания/расчёта/факта, затем восстановление dump в отдельную БД. Подключить разрешённые Google-источники и проверить фактические заголовки/единицы через предварительный просмотр. После этого зафиксировать результаты в этом файле с датой и окружением.
